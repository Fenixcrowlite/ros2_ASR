# datasets

Dataset storage layout:

- `registry/` dataset registry metadata
- `raw/` unmodified source datasets
- `imported/` normalized imported assets
- `manifests/` normalized manifests (`jsonl`)
- `processed/` derived datasets
- `noise_assets/` assets for robustness/noise scenarios
