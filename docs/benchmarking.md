# Benchmarking

## Canonical Entry Point

- Local default: `make bench`
- Direct script: `python3 scripts/run_benchmark_core.py`
- Thesis suite: `make bench-suite SCENARIO=embedded`
- Derived metrics only: `make collect-metrics SCENARIO=dialog`

## Source Of Truth

- Canonical benchmark artifacts live under `artifacts/benchmark_runs/<run_id>/`.
- `results/latest_benchmark_summary.json` is the local summary pointer used by reporting/UI flows.
- `results/benchmark_results.json` and `results/benchmark_results.csv` are temporary derived compatibility exports generated from canonical run artifacts.
- `results/runs/<run_id>/` contains thesis-oriented schema-first exports generated by `scripts/collect_metrics.py`.

## Expected Outputs

- `manifest/run_manifest.json`
- `metrics/results.json`
- `metrics/results.csv`
- `reports/summary.json`
- `reports/summary.md`
- local pointers in `results/`
- `results/runs/<run_id>/manifest.json`
- `results/runs/<run_id>/utterance_metrics.csv`
- `results/runs/<run_id>/summary.csv`
- `results/runs/<run_id>/summary.json`
- `results/runs/<run_id>/plots/*.png`

## Rules

- Benchmarks select providers only through provider profiles.
- Reports should read canonical summary artifacts, not archived legacy schemas as the primary source.
- Thesis exports are derived artifacts; they must not replace canonical `artifacts/benchmark_runs/<run_id>/` files.
- Missing energy, accent, OOV, and calibration inputs stay empty or are marked unavailable; they are not fabricated.
