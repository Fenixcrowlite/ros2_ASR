# Runtime Capture Status

Runtime graph capture was not regenerated on 2026-04-13 because another managed ASR stack from this workspace was already running.

Stop the live stack and run `make arch` to replace the placeholder `runtime_graph.json` with a fresh runtime snapshot.
