# Configs Overview

Каталог: `configs/`.

Ключевые файлы:

- `default.yaml`
- `live_mic_whisper.yaml`
- `commercial.example.yaml`
- `commercial.yaml` (локальный, не в git)

## Merge logic

См. [[03_Core/Config_Loading]].
