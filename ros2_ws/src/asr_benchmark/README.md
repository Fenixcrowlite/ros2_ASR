# asr_benchmark (Legacy)

This package is retained for backward compatibility.

Primary benchmark baseline has moved to:
- `asr_benchmark_core`
- `asr_benchmark_nodes`

New development should target the new benchmark packages.
