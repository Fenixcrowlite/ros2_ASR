# TODO: asr_launch

- [ ] Add namespace prefix argument to all launch files for multi-instance deployments.
- [ ] Add composable launch variants for low-latency local mode.
- [ ] Add benchmark autostart helper for non-interactive CI runs.
