# asr_ros (Legacy)

This package is retained for backward compatibility.

Primary runtime baseline has moved to:
- `asr_runtime_nodes`
- `asr_launch`

New development should target the new runtime packages.
