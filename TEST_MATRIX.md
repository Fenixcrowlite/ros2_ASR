# TEST_MATRIX

| Компонент | Что проверяется | Тип теста | Позитивный сценарий | Негативный сценарий | Edge-case | Статус покрытия |
|---|---|---|---|---|---|---|
| `asr_config/validation.py` | Runtime profile guards | Unit | Валидный runtime payload принимается | Некорректные `audio/vad/orchestrator/session` значения отвергаются | `vad.min_segment_ms > vad.max_segment_ms` | Covered |
| `asr_core/backend.py` | Streaming contract honesty | Unit | Native streaming backends keep explicit implementations | Backend without streaming support raises `NotImplementedError` | PCM wrapping utility tested independently | Covered |
| `asr_runtime_nodes/audio_input_node.py` | Audio source execution semantics | Unit | `file` и `mic` режимы работают явно | `audio.source=auto` и mic-open failures не маскируются fallback-переходом | Duplicate active start stays idempotent | Covered |
| `asr_ros/audio_capture_node.py` | Legacy compatibility capture semantics | Unit | `file` и `mic` режимы остаются явными | `mic` failure больше не падает назад на `file` | `input_mode=auto` отвергается как unsupported | Covered |
| `asr_ros/asr_server_node.py` | Legacy compatibility server honesty | Unit + ROS integration | One-shot service остается рабочим | Batch-only backend streaming дает явную ошибку вместо fallback-магии | Hidden cloud fallback path removed | Covered |
| `asr_backend_whisper/backend.py` | Strict device selection | Unit + component | Явно заданный `device=cpu`/`device=cuda` сохраняется как requested execution mode | CUDA runtime error больше не уводит сценарий в скрытый CPU run | Helpful libcublas guidance preserved in error text | Covered |
| `asr_provider_whisper/whisper_provider.py` | Whisper config honesty | Component | Валидный Whisper provider profile создается | Legacy `allow_cpu_fallback` setting отвергается явно | Advanced UI override with removed flag no longer passes silently | Covered |
| `asr_backend_google/backend.py` | Google model contract honesty | Unit | Валидный requested model выполняется как requested | Unsupported `model + language` pair больше не retries `default` | Error code distinguishes unsupported model from generic runtime failure | Covered |
| `asr_config/validation.py` | Benchmark profile guards | Unit | Минимальный benchmark profile без лишних секций принимается | Явно плохие `batch/streaming` поля отвергаются | Omitted optional sections do not fail | Covered |
| `asr_datasets/manifest.py` | Reproducible manifest path resolution | Unit | Relative `audio_path` resolves from the manifest directory | Missing relative file is surfaced explicitly later in the pipeline instead of being mis-resolved via caller `cwd` | Absolute paths continue to work unchanged | Covered |
| `asr_datasets/importer.py` | Uploaded dataset bundle integrity | Unit | Manifest bundle import rewrites audio references to saved bundle paths deterministically | Duplicate normalized filenames and manifest references to missing uploaded audio are rejected | Basename-only manifest references stay portable after import | Covered |
| `asr_runtime_nodes/asr_orchestrator_node.py` | `RecognizeOnce` provider override | Unit + ROS integration | Запрос с `provider_profile` использует нужный provider | Ошибка временного provider correctly bubbles up | Override does not mutate active runtime provider | Covered |
| `asr_runtime_nodes/asr_orchestrator_node.py` | Runtime status truthfulness | Unit | Local provider returns coherent ready/status payload | Broken provider validation flips credential flag false | Network provider without valid config is not reported ready | Covered |
| `asr_gateway/main.py` | Network exposure defaults | Unit | Gateway defaults to `127.0.0.1` | Explicit LAN mode still required for wide bind | Launch and server defaults stay aligned | Covered |
| `ros2_ws/src/asr_launch/launch/*.launch.py` | Launch defaults | Unit | Runtime/gateway launch files use loopback default | Wrong host default fails test | `gateway_with_runtime` and `full_stack_dev` stay consistent | Covered |
| `scripts/run_demo.sh` | Primary demo entry point | Shell + live runtime | Starts `asr_launch/runtime_minimal.launch.py` and serves `/asr/runtime/recognize_once` | Conflicting managed stack is rejected before rebuild/launch | Environment bootstrap preserved | Covered |
| `scripts/open_live_test_terminals.sh` | Primary live helper flow | Unit + shell syntax | Starts runtime stack, waits for service, subscribes to final results | Invalid input mode/file path rejected | Service/topic names reflect current runtime contract | Covered |
| `scripts/open_live_test_terminals.sh` | Manual launch guardrails | Shell syntax + manual negative checks | `mic|file` accepted | `INPUT_MODE=auto` and `provider_stream` on non-streaming provider are rejected before launch | No terminal windows open for invalid combos | Covered |
| `scripts/live_sample_eval.py` | Legacy compatibility tool guardrails | Unit + manual negative checks | Valid core/service/action runs still execute | Batch-only targets are rejected for `ros_action --action-streaming` | `language-mode=auto` no longer falls back silently to config | Covered |
| `scripts/live_sample_eval.py` | Compatibility scenario execution | Manual runtime validation | `core`, `ros_service`, `ros_action`, `ros_action --action-streaming` all produce artifacts for `mock` backend | Unsupported streaming combos fail before side effects | Isolated `ROS_DOMAIN_ID` avoids mixing with active UI stack | Covered |
| `scripts/source_runtime_env.sh` | Overlay determinism | Integration | Defaults to `ros2_ws/install` | Missing workspace overlay does not silently switch to top-level `install/` | `rqt` env check sees `asr_interfaces` only from the intended workspace | Covered |
| `Makefile` + `scripts/release_check.sh` | Release gate integrity | Unit + runtime | `test-unit`, `test-ros`, `test-colcon`, `bench` all run | Broken `colcon test` CLI is caught | Unsupported `--symlink-install` no longer used with `colcon test` | Covered |
| Package `test/test_smoke.py` surfaces | `colcon test` package health | Package smoke | Every package has at least one executable smoke test | Missing test surface no longer yields empty-package failure | Import-only smoke keeps cost low | Covered |
| CLI scripts in `scripts/` | Repo-local import bootstrap | Integration | `generate_report`, `generate_plots`, dataset import, export summary, profile validation work from temp `cwd` | Missing input file/path fails with explicit message instead of traceback | No external `PYTHONPATH` required | Covered |
| `asr_benchmark_core/orchestrator.py` | Benchmark artifact pipeline | Component + regression | Run artifacts and summaries are produced | Invalid streaming provider is rejected | Minimal benchmark profile still runs | Covered |
| `asr_benchmark_core/orchestrator.py` | Merged override validation | Component | Valid request-level benchmark overrides are accepted | Invalid merged `streaming.chunk_ms` is rejected in core orchestration too | Validation does not rely only on gateway preflight | Covered |
| `tests/integration/test_ros_recognize_service.py` | ROS runtime service surface | ROS integration | `/asr/runtime/recognize_once` success path | Missing WAV/provider failure paths are surfaced | Legacy and modular runtime flows both still exist | Partial |
| ROS integration harness | Isolation from user-launched stacks | Test infrastructure | Tests run on dedicated `ROS_DOMAIN_ID` | Shared-domain retained messages no longer corrupt assertions | Unsourced environments skip ROS-only tests honestly | Covered |
| `tests/e2e/test_web_ui_flows.py` | Browser control plane smoke | E2E | Dashboard/runtime/benchmark/logs pages load and basic actions work | Startup failure surfaces quickly | Free TCP port allocation avoids flaky collision | Covered |
| `asr_gateway` live HTTP surface | Real runtime/gateway smoke | Runtime smoke | `/api/health`, `/api/runtime/status`, `/api/runtime/live`, `/api/runtime/recognize_once` return valid responses | Unavailable gateway/runtime would fail curl smoke | Loopback bind + one-shot transcript path verified | Covered |
| `tools/docsbot` | Docs automation honesty | Tool tests + CLI runtime | Detect/snapshot/generate/validate work and write docs | Forced `openai` without key fails; no hidden mock fallback in `auto` | Template-only mode emits explicit warning and skips `LLM Draft` sections | Covered |
| `tools/archviz/runtime_extract.py` + `tools/archviz/cli.py` | Runtime graph extraction isolation | Unit + tooling CLI | `archviz runtime/all` works when the workspace is clean | A live managed stack from the same workspace forces an immediate non-zero fail-fast instead of hanging or mixing graph evidence | Conflict report includes explicit PIDs and commands | Covered |
| `scripts/secret_scan.sh` | Release secret preflight | Manual release validation | Tracked files scan passes on current repo | Real key-shaped material would fail packaging | Sanitized Google service-account fixtures no longer trip false positives | Covered |
| Cloud providers | Real cloud credentials/network path | Manual/integration | N/A in default environment | Missing creds/network should fail fast | Provider-specific auth edge cases need credentialed env | Partial |
| Static analysis | Style/type/security debt visibility | Static analysis | Findings are reported for backlog | `ruff`, `mypy`, `bandit` remain non-green | Counts recorded in audit artifacts | Partial |
